version: '3.8'

services:
  # MCP SSE Server (backend)
  # Production configuration with full security hardening
  sequential-thinking-sse:
    image: mcp/sequentialthinking:secure
    container_name: sequential-thinking-sse
    build:
      context: ../..
      dockerfile: src/sequentialthinking/Dockerfile
      platforms:
        - linux/amd64
        - linux/arm64
    user: "1000:1000"

    # READ-ONLY ROOT FILESYSTEM (immutable container)
    read_only: true

    # Temporary filesystems (in-memory, cleared on restart)
    tmpfs:
      - /tmp:mode=1777,size=100m,uid=1000,gid=1000
      - /run:mode=0755,size=10m,uid=1000,gid=1000

    # Persistent log volume (survives container restarts)
    volumes:
      - ./logs:/app/logs:rw

    # Environment variables
    environment:
      - PORT=3001
      - DISABLE_THOUGHT_LOGGING=false
      - MAX_THOUGHTS_PER_SESSION=10000
      - MAX_THOUGHT_LENGTH=10000
      - SESSION_TIMEOUT_MS=3600000
      - NODE_ENV=production

    # Security options
    security_opt:
      - no-new-privileges:true

    # Capabilities (minimal required set)
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
      - CHOWN
      - SETGID
      - SETUID

    # Resource limits
    mem_limit: 1g
    memswap_limit: 1g
    mem_reservation: 512m
    cpus: 1.0
    pids_limit: 100

    # Command (uses Dockerfile default: node dist/index.js sse)
    # No override needed - using default from Dockerfile

    # Restart policy
    restart: unless-stopped

    # Network
    networks:
      - mcp_network

    # Health check
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3001/health', (res) => process.exit(res.statusCode === 200 ? 0 : 1)).on('error', () => process.exit(1))"]
      interval: 30s
      timeout: 3s
      start_period: 5s
      retries: 3

networks:
  mcp_network:
    driver: bridge
